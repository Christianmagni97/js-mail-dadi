const number = [];
for (let index = 0 ; index < 6 ; index++){
    const userNumber = Number.parseInt( prompt('Type a number'), 10);
    if (userNumber % 2 numbersArray. push(userNumber);) {
        number. push(userNumber);
    }
}    